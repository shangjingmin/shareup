= form_for @metadoc do |f|
  -if @metadoc.errors.any?
    #error_explanation
      %h2= "#{pluralize(@metadoc.errors.count, "error")} prohibited this metadoc from being saved:"
      %ul
        - @metadoc.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :from_url, 'From'
    = f.text_field :from_url
    = f.check_box :public
    = f.label :public
  .field
    = f.label :title
    = f.text_field :title
    = f.label :tags
    = f.text_field :tags, :size=>20
  .field
    = f.text_area :content, :placeholder=>'Content...', :rows=>12, :cols=>50
  .field
    = f.label :comment
    = f.text_field :comment
  .actions
    = f.submit 'Save'
